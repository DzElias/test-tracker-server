version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      # Variables sensibles se inyectarán manualmente
    restart: unless-stopped
    volumes:
      - ./config/.env:/app/.env  # Montar archivo .env externo